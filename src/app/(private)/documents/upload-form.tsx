"use client";import{useState}from"react";import{useRouter}from"next/navigation";export function UploadForm(){const[error,setError]=useState('');const router=useRouter();return <form className="resource-form" onSubmit={async e=>{e.preventDefault();const res=await fetch('/api/documents',{method:'POST',body:new FormData(e.currentTarget)});if(!res.ok){setError('Seleccioná un PDF de hasta 10 MB.');return}(e.target as HTMLFormElement).reset();router.refresh()}}><label>Nombre<input name="name" required/></label><label>Categoría<select name="category"><option>programas de grado</option><option>reglamentos</option><option>katas</option><option>kihon</option><option>otro</option></select></label><label>Versión<input name="version"/></label><label>Archivo PDF<input name="file" type="file" accept="application/pdf" required/></label>{error&&<p role="alert" className="form-error">{error}</p>}<button>Subir documento</button></form>}
