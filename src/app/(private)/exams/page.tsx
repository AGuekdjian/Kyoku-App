import{connectDb}from"@/lib/db";import{Exam}from"@/models/Exam";import{ResourceForm}from"@/components/resource-form";export const dynamic='force-dynamic';export default async function Exams(){await connectDb();const items=await Exam.find({deletedAt:null}).sort({date:-1}).lean();return <><header><div><p className="eyebrow">Evaluaciones</p><h1>Exámenes</h1><p>Aprobaciones, pendientes y revisiones.</p></div></header><details className="panel"><summary>Crear examen</summary><ResourceForm endpoint="/api/exams" fields={[{name:'name',label:'Nombre',required:true},{name:'date',label:'Fecha',type:'date',required:true},{name:'location',label:'Lugar'},{name:'examiner',label:'Examinador'},{name:'status',label:'Estado',options:[{value:'DRAFT',label:'Borrador'},{value:'SCHEDULED',label:'Programado'}]}]}/></details><section className="panel">{items.map(e=>{const pending=(e.registrations as unknown as {observations:{status:string}[]}[]).flatMap(r=>r.observations).filter(o=>o.status==='PENDING').length;return <div className="list-row" key={String(e._id)}><div><strong>{String(e.name)}</strong><small>{new Date(e.date as Date).toLocaleDateString('es-UY')} · {String(e.status)}</small></div><span className="badge">{(e.registrations as unknown[]).length} inscriptos · {pending} pendientes</span></div>})}</section></>}
