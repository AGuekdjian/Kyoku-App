import{redirect}from"next/navigation";import{getSession}from"@/features/auth/session";import{connectDb}from"@/lib/db";import{AuditLog}from"@/models/AuditLog";export const dynamic='force-dynamic';export default async function Audit(){if((await getSession())?.role!=='ADMIN')redirect('/');await connectDb();const items=await AuditLog.find().sort({timestamp:-1}).limit(100).populate('actorId','name').lean();return <><header><div><p className="eyebrow">Trazabilidad</p><h1>Auditoría</h1></div></header><section className="panel">{items.map(i=><div className="list-row" key={String(i._id)}><div><strong>{String(i.action)}</strong><small>{String(i.entity)} · {new Date(i.timestamp as Date).toLocaleString('es-UY')}</small></div><span>{String((i.actorId as unknown as {name:string})?.name??'Sistema')}</span></div>)}</section></>}
