import{connectDb}from"@/lib/db";import{Student}from"@/models/Student";import{Grade}from"@/models/Grade";import{ResourceForm}from"@/components/resource-form";import{calculateAge}from"@/features/students/domain/age";export const dynamic='force-dynamic';
export default async function Students(){await connectDb();const[students,grades]=await Promise.all([Student.find({deletedAt:null}).populate('currentGradeId','name').sort({lastName:1}).limit(100).lean(),Grade.find({active:true,deletedAt:null}).sort({order:1}).lean()]);return <><header><div><p className="eyebrow">Personas</p><h1>Alumnos</h1><p>{students.length} registros visibles</p></div></header><details className="panel"><summary>Nuevo alumno</summary><ResourceForm endpoint="/api/students" fields={[{name:'firstName',label:'Nombre',required:true},{name:'lastName',label:'Apellido',required:true},{name:'birthDate',label:'Fecha de nacimiento',type:'date',required:true},{name:'phone',label:'Teléfono',required:true},{name:'emergencyContact',label:'Contacto de emergencia',required:true},{name:'joinedAt',label:'Fecha de ingreso',type:'date',required:true},{name:'currentGradeId',label:'Grado',required:true,options:grades.map(g=>({value:String(g._id),label:String(g.name)}))},{name:'weight',label:'Peso (kg)',type:'number'},{name:'height',label:'Altura (cm)',type:'number'}]}/></details><section className="panel"><div className="table-wrap"><table><thead><tr><th>Alumno</th><th>Edad</th><th>Grado</th><th>Contacto</th><th>Estado</th></tr></thead><tbody>{students.map(s=><tr key={String(s._id)}><td><strong>{String(s.lastName)}, {String(s.firstName)}</strong></td><td>{calculateAge(s.birthDate as Date)}</td><td>{String((s.currentGradeId as unknown as {name:string})?.name??'—')}</td><td>{String(s.phone)}</td><td><span className="badge">{s.active?'Activo':'Inactivo'}</span></td></tr>)}</tbody></table></div></section></>}
