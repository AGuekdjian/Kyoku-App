import { NextRequest,NextResponse } from "next/server";import { connectDb } from "@/lib/db";import { apiError } from "@/lib/http";import { requireSession } from "@/features/auth/session";import { studentSchema } from "@/features/students/schemas/student";import { Student } from "@/models/Student";import { audit } from "@/features/audit/service";
type C={params:Promise<{id:string}>};export async function PATCH(req:NextRequest,{params}:C){try{const session=await requireSession();await connectDb();const{id}=await params;const input=studentSchema.partial().parse(await req.json());const update={...input,...(input.weight!==undefined?{weightUpdatedAt:new Date()}: {})};const item=await Student.findOneAndUpdate({_id:id,deletedAt:null},update,{new:true,runValidators:true});if(!item)throw new Error('NOT_FOUND');await audit({actorId:session.userId,action:'student.update',entity:'Student',entityId:id});return NextResponse.json(item)}catch(e){return apiError(e)}}export async function DELETE(_:NextRequest,{params}:C){try{const session=await requireSession();await connectDb();const{id}=await params;const item=await Student.findOneAndUpdate({_id:id,deletedAt:null},{deletedAt:new Date(),active:false});if(!item)throw new Error('NOT_FOUND');await audit({actorId:session.userId,action:'student.delete',entity:'Student',entityId:id});return new NextResponse(null,{status:204})}catch(e){return apiError(e)}}
