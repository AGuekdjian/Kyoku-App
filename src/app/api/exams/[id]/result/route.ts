import{NextRequest,NextResponse}from"next/server";import{z}from"zod";import{connectDb}from"@/lib/db";import{apiError}from"@/lib/http";import{requireSession}from"@/features/auth/session";import{examResults}from"@/features/exams/domain/result";import{recordExamResult}from"@/features/exams/service";
const schema=z.object({registrationId:z.string(),result:z.enum(examResults),observations:z.array(z.object({category:z.string().min(1),description:z.string().min(1)})).default([])});export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const session=await requireSession();await connectDb();const body=schema.parse(await req.json());return NextResponse.json(await recordExamResult({examId:(await params).id,actorId:session.userId,...body}))}catch(e){return apiError(e)}}
