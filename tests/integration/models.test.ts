import{afterAll,beforeAll,describe,expect,it}from"vitest";import mongoose from"mongoose";import{MongoMemoryServer}from"mongodb-memory-server";import{Grade}from"@/models/Grade";import{Student}from"@/models/Student";
describe('mongoose integration',()=>{let mongo:MongoMemoryServer;beforeAll(async()=>{mongo=await MongoMemoryServer.create();await mongoose.connect(mongo.getUri('kyoku_test'))},60000);afterAll(async()=>{await mongoose.disconnect();await mongo.stop()});it('persists relations while soft-deleted students remain historical',async()=>{const grade=await Grade.create({name:'10.º Kyu',order:1,type:'KYU'});const student=await Student.create({firstName:'Test',lastName:'Ficticio',birthDate:new Date('2010-01-01'),phone:'000000',emergencyContact:'Demo',joinedAt:new Date(),currentGradeId:grade._id});student.deletedAt=new Date();student.active=false;await student.save();expect(await Student.countDocuments({_id:student._id})).toBe(1);expect(await Student.countDocuments({_id:student._id,deletedAt:null})).toBe(0)})});
